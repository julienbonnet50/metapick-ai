interface SeoProps {
  title: string;
  description: string;
  url: string;
  image: string;
  color?: string; // Optional color for Discord embed
  siteName?: string; // Optional site name for richer embeds
}